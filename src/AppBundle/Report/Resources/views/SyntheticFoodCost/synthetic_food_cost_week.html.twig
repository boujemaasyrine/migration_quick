<tr class="total-week mainRow">
    <td>Total</td>
    <td class="font-reduce">{{ week.ca_brut_ttc|float_format }}</td>
    <td class="font-reduce">{{ week.ca_net_ht|float_format }}</td>
    <td class="font-reduce">{{ week.ventes_pr|float_format }}</td>
    <td class="font-reduce">{{ week.fc_mix|float_format }}</td>

    <!-- Theorique -->
    <td class="font-reduce">{{ week.fc_ideal|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_i_inv|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_inv_pourcentage|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_i_vtes|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_vtes_pourcentage|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_connues|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_connues_pourcentage|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_inconnues|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_inconnues_pourcentage|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_totales|float_format }}</td>
    <td class="font-reduce">{{ week.pertes_totales_pourcentage|float_format }}</td>
    <td class="font-reduce">{{ week.fc_theo|float_format }}</td>
    <td class="font-reduce">{{ week.marge_theo|float_format }}</td>

    <!-- Real -->
    <td class="font-reduce">{{ week.initialStock|float_format }}</td>
    <td class="font-reduce">{{ week.entree|float_format }}</td>
    <td class="font-reduce">{{ week.sortie|float_format }}</td>
    <td class="font-reduce">{{ week.finalStock|float_format }}</td>
    <td class="font-reduce">{{ week.conso_real|float_format }}</td>
    <td class="font-reduce">{{ week.fc_real|float_format }}</td>
    <td class="font-reduce">{{ week.marge_real|float_format }}</td>

    <!-- Marge -->
    <td class="font-reduce">{{ week.br|float_format }}</td>
    <td class="font-reduce">{{ week.br_pourcentage|float_format }}</td>
    {#<td>{{ week.discount|float_format }}</td>#}
    <td class="font-reduce">{{ week.pr_pub|float_format }}</td>
    <td class="font-reduce">{{ week.discount_pourcentage|float_format }}</td>
    <td class="font-reduce">{{ week.fc_real_net|float_format }}</td>
    <td class="font-reduce">{{ week.marge_brute|float_format }}</td>
</tr>